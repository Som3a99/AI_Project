Order = W -> N -> S -> E
push the start cell in Forntier and Explored
Repeat until Goal is reached or Forntier is Empty:

    current_cell = Forntier.pop=
    for each direction(ESNW):
        child_cell = Next possible cell
        if child_cell already in Explored list -> Do nothing
        otherwise -> Append/push child_cell to both Explored & Forntier

        E -> child_cell =
        S -> child_cell =
        N -> child_cell =
        W -> child_cell =
